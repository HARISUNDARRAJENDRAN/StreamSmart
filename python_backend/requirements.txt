# Core FastAPI and server dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
python-multipart==0.0.6

# Database (install when MongoDB is available)
pymongo==4.6.0
motor==3.3.2

# YouTube and media processing (install when yt-dlp is available)
youtube-transcript-api==0.6.2
yt-dlp==2024.12.13

# HTTP and utilities
requests==2.31.0
python-dotenv==1.0.0

# Data processing - NumPy MUST be < 2.0 for TensorFlow 2.15 compatibility
numpy>=1.24.0,<2.0.0
pandas==2.1.4
scikit-learn==1.3.2

# AI and ML - Core packages (always install)
transformers==4.35.2
sentence-transformers>=2.3.0
huggingface-hub>=0.20.0

# PyTorch - CPU-only wheels for compatibility
torch==2.3.1
torchvision==0.18.1
torchaudio==2.3.1

# Audio processing with timestamps
whisper-timestamped>=1.14.0

# TensorFlow with compatibility constraints (optional - large download)
# tensorflow>=2.16,<2.20
# ml-dtypes<0.5

# Google AI services (optional - install when API keys are available)
# google-generativeai==0.3.1

# Additional utilities (optional)
# nltk==3.8.1
# redis==5.0.1
# apscheduler==3.10.4

# Image processing
pillow

# Video processing (optional - install when needed)
# opencv-python